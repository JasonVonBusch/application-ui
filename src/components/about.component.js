import React, {Component} from "react";

export default class About extends Component {
    render() {
        return(
            <div>
                <h1>About</h1>
                <p>This is a ReactJS application. This application was built after running "npm create-react-app" and removing the components that aren't being used.</p>
            </div>
        );
    }
}